fastapi
uvicorn
# Opcional: driver del backend de retrieval Neo4j (modo fulltext). Sin este
# paquete o sin el contenedor, la app sirve con GraphIndex in-memory y lo
# declara como fallback en /runs (ver README, "Backend de retrieval por grafo").
# neo4j
# harness.py (GraphAgent) importa anthropic; misma versión que el repo raíz.
# El extra [bedrock] agrega boto3/botocore para el modo Bedrock (H2);
# verificado que 0.100.0 incluye AnthropicBedrock funcionando.
anthropic[bedrock]==0.100.0
