# Servidor MCP del KG (mcp_kg/). Se instala sobre el .venv del repo (Python 3.10).
# Pinneado a lo que corrió en U-A2.0-banco. `neo4j` es el driver ya pinneado en
# data/experiment/neo4j/requirements.txt (mismo número).
neo4j==6.2.0
mcp==1.29.0
