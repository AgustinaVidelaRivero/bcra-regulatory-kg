# Re-test de C3 — Umbral propagado de compañías financieras (2026-08-02)

Aplicación: BKL-0023 / propuesta sellada
`data/backlog/propuestas/C3_umbral_companias.md` (commit `6cca6c9`); primera
corrección de origen 100 % diagnóstico-automático (verificador validado, gate
U5). Grafo: `data/experiment/grafo_v2/reensamblado_v3/kg.json` (vigente,
in-place). Convención de identidad: **opción A sin remapeo** — la descripcion
no cambia → el id `…_7bb7bb` queda intacto → cero aristas tocadas. Árbitro:
PDF pág. 4, tabla estructural del 1.2 (`Bancos | Restantes entidades (salvo
Cajas de Crédito Cooperativas)` → `5.000 | 2.500`) + oración de compañías
financieras. Scripts en el scratchpad de sesión.

## Estado base

- Post-C2: sha256 `ddf7ff8ad08be545c3c9c821704c7c9d9a3f1c20bfab7acaa7ff03bc3dca3127`,
  4.459 nodos / 8.046 aristas (== precondición; C2 commiteada, mismo sha que
  cierra su re-test).
- Round-trip JSON verificado antes de escribir: `json.dumps(load(raw),
  ensure_ascii=False, indent=2) == raw` (sin newline final) — la escritura
  in-place no re-formatea nada.

## Aplicación

ÚNICO cambio en todo el archivo:
`properties.umbral` del nodo
`Restriccion_las_companias_financieras_que_realicen_en_forma_directa_operaciones_de_comercio__7bb7bb`:
`"2.500 millones de pesos"` → `"5.000 millones de pesos"`.
sha256 aplicado: `d673dd722a11da67c0fe1f18a686aeaa3f8eeaed29e19e2601e39fe58e2541b2`.

## Re-test (pre-especificado en la propuesta)

| Paso | Resultado |
|---|---|
| (a) `properties.umbral` post == columna "Bancos" de la tabla estructural del PDF pág. 4 (leída de nuevo con extract_tables en el propio re-test) | **PASS** — PDF `5.000` → nodo `"5.000 millones de pesos"` |
| (b) `descripcion` e `id` byte-idénticos al pre | **PASS** |
| (c) conteos sin cambio (4.459 / 8.046) y cero cambios en todo otro objeto (comparación objeto a objeto contra la copia pre) | **PASS** — 1 nodo con diff, 0 aristas con diff, único delta de properties = `umbral` |
| (d) shapes delta 0 (`scripts/shapes_validator.py --kg …` pre vs post, diff de salidas completas) | **PASS** — diff vacío |

Nota (d): la salida del validador porta los FAIL conocidos del desfase
matriz-run_3 vs esquema v2 (S1: 82 `subclase_de`, ítem pendiente de la cola
del tablero §5.2); el criterio del re-test es **delta 0**, y las salidas pre
y post son idénticas byte a byte.

Incidente registrado (sin efecto sobre el veredicto): `shapes_validator.py`
escribe por default un reporte en `reports/shapes_run_3_v0.md` (`--out`,
`scripts/shapes_validator.py:82`); las dos corridas de esta unidad lo
sobrescribieron — escritura fuera del mandato, revertida con
`git checkout -- reports/shapes_run_3_v0.md` antes del cierre. El criterio
(d) se evaluó sobre las salidas de stdout capturadas en el scratchpad, no
sobre ese archivo.

## Veredicto

Re-test **(a)-(d) PASS (4/4)**. La corrección queda aplicada sobre el vigente
y lista para el commit de la autora (un commit por corrección). Cero
referencias históricas rotas: no hubo ids nuevos ni remapeo.
