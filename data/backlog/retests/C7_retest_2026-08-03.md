# Re-test de C7 — Calificadores del 7.1 de RegInf (2026-08-03)

Aplicación: BKL-0005 (expediente E5, `data/backlog/expediente_retriage_v3.md#E5`).
Corrección quirúrgica de una sola propiedad: la propuesta se redujo al
antes/después laudado en sesión (diff de dos inserciones) y queda documentada
íntegra en la sección "Aplicación" de este re-test — no se creó archivo de
propuesta separado. Grafo: `data/experiment/grafo_v2/reensamblado_v3/kg.json`
(vigente, in-place). Árbitro: el PDF
(`data/experiment/subset/TO_regimen_informativo_contable_mensual_actual.pdf`,
punto 7.1 "Normas de procedimiento", página 35), releído con `pypdf` dentro
del propio re-test. Scripts y salidas completas en el scratchpad de sesión
(paquete `revision_C7/`).

## Estado base

- Post-C6: sha256 `fe5f6b69d3b36fdf0cd79b228a03aad96f2de33bb1a188ecf0add77c433abd88`,
  4.469 nodos / 8.073 aristas (== precondición del mandato; mismo sha que
  cierra el re-test de C6). Verificación: `shasum -a 256 <kg>` + one-liner de
  conteo del tablero.
- Round-trip JSON verificado antes de escribir: `json.dumps(json.loads(raw),
  ensure_ascii=False, indent=2).encode('utf-8') == raw` — BYTE-IDÉNTICO; la
  escritura in-place no re-formatea nada.

## Aplicación

Edición de **un solo campo** (`properties.descripcion`) de **un solo nodo**:
`Obligacion_para_el_calculo_del_importe_correspondiente_al_mes_n_procedera_tenerse_en_cuenta_425c6b`
(type `Obligacion`, label "Aplicar esquema de cálculo para disminución de
exigencia", provenance "Punto 7.1. Normas de procedimiento", `rol_fuente:
"cuerpo"` — sin cambio: la spec M7 no se ve afectada). Ningún otro campo del
nodo, ningún otro nodo y ninguna arista se tocaron.

Defecto (especie `amputacion`): la enumeración del esquema "Posición mes n"
del 7.1 perdió dos calificadores en la extracción — la RPC perdió "informada
en el mes n" y la Franquicia perdió "calculada según datos del mes n".

`descripcion` ANTES:

> Para el cálculo del importe correspondiente al mes n procederá tenerse en
> cuenta el siguiente esquema: Datos exigencia riesgo de crédito del mes n
> (incluyendo INC), Exigencia por riesgo de mercado del último día del mes n,
> Exigencia por riesgo operacional del mes n, Responsabilidad Patrimonial
> Computable, Franquicia informada en el mes n

`descripcion` DESPUÉS (laudada; mínimo cambio, dos inserciones):

> Para el cálculo del importe correspondiente al mes n procederá tenerse en
> cuenta el siguiente esquema: Datos exigencia riesgo de crédito del mes n
> (incluyendo INC), Exigencia por riesgo de mercado del último día del mes n,
> Exigencia por riesgo operacional del mes n, Responsabilidad Patrimonial
> Computable **informada en el mes n**, Franquicia informada en el mes n
> **calculada según datos del mes n**

Fuente (PDF, página 35, renglones del esquema, verbatim en
`verbatim_7_1_reginf_pagina35.txt` del paquete):

```
Responsabilidad Patrimonial Computable informada en el mes n
Franquicia informada en el mes n calculada según datos del mes n
```

- Conteos: 4.469/8.073 → **4.469/8.073** (sin cambio, == expectativa del
  mandato).
- sha256 aplicado: `26fac8b49f6c08c1aa364b47273d36958d831f240d4e6b4ee7700b6a0bff3571`.
- `git diff` contra HEAD: 1 archivo, **1 línea** (+1/−1): la `descripcion`.

## Re-test (ejecutado determinístico, 0 API)

27 checks, **27/27 PASS**. Script: `retest_C7.py` del paquete (corre desde la
raíz del repo); salida completa check por check:
`retest_C7_salida_completa.txt` del paquete.

| Capa | Resultado |
|---|---|
| (a) chunk-contra-PDF: prefijo + los 5 renglones del esquema presentes en el 7.1 releído (normalización: des-guionado + eliminación de espacios — neutraliza artefactos de `pypdf` tipo "correspondient e" — + casefold + sin acentos); los dos calificadores presentes y pegados a su portador correcto (RPC «informada en el mes n», Franquicia «calculada según datos del mes n») | **PASS** (8/8) |
| (b) estructural: conteos sin cambio 4.469/8.073; mismo conjunto de ids; los 4.468 nodos restantes byte-idénticos vs HEAD (comparación canónica nodo a nodo); las 8.073 aristas byte-idénticas incluido el orden; el nodo objetivo difiere SOLO en `properties` y dentro de `properties` SOLO en `descripcion`; descripcion HEAD == ANTES laudado y post == DESPUÉS laudado; `rol_fuente` sigue `cuerpo` | **PASS** (9/9) |
| (c) sondas del expediente: «calculada según datos del mes» HEAD 0 hits → post 1 hit, en el nodo objetivo; «Responsabilidad Patrimonial Computable informada» HEAD 0 → post 1, mismo nodo | **PASS** (3/3) |
| (d) RT-1..RT-3 nuevas (EV1-005 QUEMADA, excluida): gold-presente (calificadores en la `descripcion` del portador) + portador-alcanzable con el `GraphIndex` real de `harness.py` sobre el kg post — RT-1 «esquema cálculo importe mes n disminución exigencia franquicia» rank 1, RT-2 «responsabilidad patrimonial computable cálculo importe correspondiente al mes» rank 1, RT-3 «franquicia importe correspondiente al mes n cálculo esquema» rank 1; `ver_nodo`(portador) devuelve la `descripcion` corregida íntegra | **PASS** (7/7) |

Nota de alcance de (d): `buscar_nodos` rankea por tokens de `label + id` — los
calificadores restaurados viven en `descripcion` y no alteran ningún ranking
(coherente con (b): todo lo demás byte-idéntico); la corrección se vuelve
visible para el agente vía `ver_nodo`, verificado en el último check.

## Veredicto

Re-test **(a)-(d) PASS (27/27)**. La corrección queda aplicada sobre el
vigente y lista para el commit de la autora (un commit por corrección). Cero
nodos/aristas agregados o eliminados, cero ids renombrados: edición in-place
de una sola propiedad. La corrida real de RT-1..RT-3 con agente+juez queda
pendiente de la autora (costo de API; amortizada en la sesión API de U6,
junto con los RT de C5 y C6). EV1-005 está quemada y excluida de todo re-test.
